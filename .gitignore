.DS_Store
.env
.env.local
*.log
node_modules/
dist/
build/
.vscode/
.idea/

# secrets / private
secrets/
*.pem
*.key
credentials.json
google-service-account.json
*-service-account.json

# pipeline working files (regenerated each run; large + intermediate)
leads/pipeline/data/

# NOTE: master-list.csv and competitors.csv ARE committed.
# They contain only public Google Business Profile data (name, phone,
# website, review count, address). No PHI, no patient data. Public commit
# enables IMPORTDATA into Google Sheets without a service account.

# customer data — never commit
customer-data/
patient-info/
*.csv.private

# generated audit PDFs
audits/*.pdf
__pycache__/
